const getters = {
  // demoScrollTop: state => state.app.demoScrollTop,
  isLoading: state => state.app.isLoading,
  direction: state => state.app.direction,
  lat: state => state.app.lat,
  lng: state => state.app.lng,
  addr: state => state.app.addr,
  province: state => state.app.province,
  city: state => state.app.city,
  district: state => state.app.district,
  status: state => state.app.status,
}
export default getters
